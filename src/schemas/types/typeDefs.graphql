type ExampleMetadata {
  owner: String!
  createdAt: String!
}

type Example {
  title: String!
  description: String
  tags: [String!]!
  isActive: Boolean!
  metadata: ExampleMetadata!
}

type Query {
  example: Example!
  searchExamples(query: String): [String!]!
}

